"use client";

import { useEffect, useMemo, useState } from "react";
import { Activity, ArrowDownRight, ArrowLeft, ArrowUpRight, Bell, CandlestickChart, CheckCircle2, ChevronDown, Clock3, Crosshair, Layers3, Maximize2, Radio, ShieldCheck, SlidersHorizontal, WalletCards, X } from "lucide-react";
import { Link } from "@/components/router-link";
import { InteractiveCandlestickChart } from "@/components/charts/interactive-candlestick";
import { useDemo } from "@/lib/demo/store";
import { useLocation } from "react-router-dom";

function money(value: number, digits = 2) { return value.toLocaleString(undefined, { minimumFractionDigits: digits, maximumFractionDigits: digits }); }

export default function MarketsPage() {
  const { pathname } = useLocation();
  const { markets, marketHistory, positions, transactions, balance, hasDeposit, openPosition } = useDemo();
  const [symbol, setSymbol] = useState(markets[0]?.symbol ?? "EUR/USD");
  const [side, setSide] = useState<"BUY" | "SELL">("BUY");
  const [quantity, setQuantity] = useState("0.10");
  const [toast, setToast] = useState<string | null>(null);
  const market = markets.find((item) => item.symbol === symbol) ?? markets[0];
  const points = marketHistory[symbol] ?? [];
  const last = points.at(-1);
  const high = points.length ? Math.max(...points.map((point) => point.high)) : market.price;
  const low = points.length ? Math.min(...points.map((point) => point.low)) : market.price;
  const change = market.price - market.previousPrice;
  const openPositions = positions.filter((position) => position.status === "OPEN" && position.symbol === symbol);
  const recentTrades = transactions.filter((transaction) => transaction.provider === symbol).slice(0, 4);

  useEffect(() => { if (!toast) return; const timer = window.setTimeout(() => setToast(null), 4200); return () => window.clearTimeout(timer); }, [toast]);
  useEffect(() => { document.documentElement.classList.add("market-terminal-route"); return () => document.documentElement.classList.remove("market-terminal-route"); }, [pathname]);
  const spread = useMemo(() => market.price * 0.00012, [market.price]);
  function submit() { const error = openPosition(symbol, side, Number(quantity)); setToast(error ?? `${side} order submitted for ${symbol}`); }

  return <main className="min-h-[calc(100vh-73px)] overflow-x-hidden bg-[#0d1110] px-3 py-4 text-white sm:px-5 lg:px-7"><div className="mx-auto max-w-[1600px]"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><Link href="/app" className="flex items-center gap-2 text-xs font-bold text-white/55 hover:text-white"><ArrowLeft className="h-4 w-4" />Workspace overview</Link><div className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.15em] text-white/45"><span className="h-2 w-2 animate-pulse rounded-full bg-[#b2de4f]" />Market stream connected <span className="hidden sm:inline">· simulated</span></div></div><div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_330px]"><section className="min-w-0 overflow-hidden rounded-2xl border border-white/10 bg-[#111716] shadow-2xl"><header className="flex flex-wrap items-center justify-between gap-3 border-b border-white/10 px-4 py-3 sm:px-5"><div className="flex min-w-0 items-center gap-3"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-[#b2de4f]/15 text-[#b2de4f]"><CandlestickChart className="h-5 w-5" /></div><div className="min-w-0"><div className="flex items-center gap-2"><select value={symbol} onChange={(event) => setSymbol(event.target.value)} className="max-w-[140px] bg-transparent text-lg font-bold text-white outline-none sm:max-w-none">{markets.map((item) => <option key={item.symbol} value={item.symbol} className="bg-[#111716]">{item.symbol}</option>)}</select><ChevronDown className="h-4 w-4 text-white/40" /></div><p className="text-[11px] text-white/45">Forex & commodities · 1.5s ticks</p></div></div><div className="flex items-center gap-2 text-right"><div><p className="text-xl font-bold tabular-nums sm:text-2xl">{market.price.toFixed(market.price > 100 ? 2 : 4)}</p><p className={`flex items-center justify-end text-xs font-bold ${change >= 0 ? "text-[#b2de4f]" : "text-red-400"}`}>{change >= 0 ? <ArrowUpRight className="h-3 w-3" /> : <ArrowDownRight className="h-3 w-3" />}{change >= 0 ? "+" : ""}{change.toFixed(4)}</p></div><button type="button" aria-label="Market alerts" className="ml-2 rounded-xl border border-white/10 p-2 text-white/55 hover:text-white"><Bell className="h-4 w-4" /></button></div></header><div className="grid grid-cols-2 border-b border-white/10 sm:grid-cols-4"><div className="border-r border-white/10 px-4 py-3"><p className="text-[10px] uppercase tracking-widest text-white/35">24h high</p><p className="mt-1 text-sm font-bold">{money(high, market.price > 100 ? 2 : 4)}</p></div><div className="border-r border-white/10 px-4 py-3 sm:border-r"><p className="text-[10px] uppercase tracking-widest text-white/35">24h low</p><p className="mt-1 text-sm font-bold">{money(low, market.price > 100 ? 2 : 4)}</p></div><div className="border-r border-white/10 px-4 py-3"><p className="text-[10px] uppercase tracking-widest text-white/35">Spread</p><p className="mt-1 text-sm font-bold">{spread.toFixed(4)}</p></div><div className="px-4 py-3"><p className="text-[10px] uppercase tracking-widest text-white/35">Volume</p><p className="mt-1 text-sm font-bold">{(last?.volume ?? 0).toLocaleString(undefined, { maximumFractionDigits: 0 })}</p></div></div><div className="border-b border-white/10 px-2 py-3 sm:px-4"><InteractiveCandlestickChart points={points} /></div><footer className="flex flex-wrap items-center justify-between gap-2 px-4 py-3 text-[10px] uppercase tracking-widest text-white/35"><span className="flex items-center gap-2"><Crosshair className="h-3.5 w-3.5" />Hover candles for OHLC</span><span className="flex items-center gap-2"><Maximize2 className="h-3.5 w-3.5" />Scroll or use zoom controls</span><span className="flex items-center gap-2"><Radio className="h-3.5 w-3.5 text-[#b2de4f]" />Live simulation</span></footer></section><aside className="space-y-4"><section className="rounded-2xl border border-white/10 bg-[#111716] p-4 shadow-2xl"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-white/40">Order ticket</p><p className="mt-1 text-lg font-bold">Place trade</p></div><SlidersHorizontal className="h-4 w-4 text-white/40" /></div><div className="mt-4 grid grid-cols-2 gap-1 rounded-xl bg-black/20 p-1"><button type="button" onClick={() => setSide("BUY")} className={`rounded-lg py-2.5 text-xs font-bold ${side === "BUY" ? "bg-[#b2de4f] text-[#111716]" : "text-white/45"}`}>BUY / LONG</button><button type="button" onClick={() => setSide("SELL")} className={`rounded-lg py-2.5 text-xs font-bold ${side === "SELL" ? "bg-red-400 text-white" : "text-white/45"}`}>SELL / SHORT</button></div><label className="mt-4 block text-xs font-bold text-white/60">Quantity<input value={quantity} onChange={(event) => setQuantity(event.target.value)} type="number" min="0.01" step="0.01" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none focus:border-[#b2de4f]" /></label><div className="mt-4 space-y-2 border-t border-white/10 pt-4 text-xs"><div className="flex justify-between"><span className="text-white/45">Estimated price</span><span>{market.price.toFixed(4)}</span></div><div className="flex justify-between"><span className="text-white/45">Available balance</span><span>KES {money(balance)}</span></div><div className="flex justify-between"><span className="text-white/45">Margin mode</span><span className="text-[#b2de4f]">Demo isolated</span></div></div><button type="button" onClick={submit} className={`mt-5 w-full rounded-xl px-4 py-3.5 text-sm font-bold ${side === "BUY" ? "bg-[#b2de4f] text-[#111716]" : "bg-red-400 text-white"}`}>{side} {symbol}</button>{!hasDeposit && <p className="mt-3 text-center text-[11px] leading-4 text-amber-300">Deposit funds before opening a simulated position.</p>}<p className="mt-3 text-[10px] leading-4 text-white/35">Trading involves risk. Simulated fills do not represent broker execution.</p></section><section className="rounded-2xl border border-white/10 bg-[#111716] p-4"><div className="flex items-center justify-between"><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-white/40">Open positions</p><Link href="/app/positions" className="text-[10px] font-bold text-[#b2de4f]">View all</Link></div>{openPositions.length ? <div className="mt-3 space-y-3">{openPositions.map((position) => <div key={position.id} className="flex items-center justify-between rounded-xl bg-black/20 p-3"><div><p className="text-xs font-bold">{position.side} {position.symbol}</p><p className="mt-1 text-[10px] text-white/40">{position.quantity.toFixed(2)} lot · entry {position.entryPrice}</p></div><p className={`text-xs font-bold ${position.unrealizedPnl >= 0 ? "text-[#b2de4f]" : "text-red-400"}`}>{position.unrealizedPnl >= 0 ? "+" : "-"}KES {Math.abs(position.unrealizedPnl).toFixed(2)}</p></div>)}</div> : <p className="mt-4 text-xs text-white/35">No open {symbol} positions.</p>}</section><section className="rounded-2xl border border-white/10 bg-[#111716] p-4"><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-white/40">Market status</p><div className="mt-3 grid grid-cols-2 gap-2 text-xs"><div className="rounded-xl bg-black/20 p-3"><Activity className="h-4 w-4 text-[#b2de4f]" /><p className="mt-2 text-white/45">Ticks</p><p className="mt-1 font-bold">Streaming</p></div><div className="rounded-xl bg-black/20 p-3"><ShieldCheck className="h-4 w-4 text-[#b2de4f]" /><p className="mt-2 text-white/45">Risk gate</p><p className="mt-1 font-bold">Enabled</p></div></div></section></aside></div><div className="mt-4 grid gap-4 lg:grid-cols-[1.2fr_1fr_1fr]"><section className="rounded-2xl border border-white/10 bg-[#111716] p-4"><div className="flex items-center gap-2"><Layers3 className="h-4 w-4 text-[#b2de4f]" /><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-white/40">Order flow</p></div><div className="mt-4 flex items-center gap-3"><div className="h-2 flex-1 rounded-full bg-[#b2de4f]" /><span className="text-xs font-bold text-[#b2de4f]">BUY 62%</span></div><div className="mt-2 flex items-center gap-3"><div className="h-2 w-[38%] rounded-full bg-red-400" /><span className="text-xs font-bold text-red-400">SELL 38%</span></div><p className="mt-4 text-[11px] text-white/35">A simulated view of current market pressure.</p></section><section className="rounded-2xl border border-white/10 bg-[#111716] p-4"><div className="flex items-center gap-2"><Clock3 className="h-4 w-4 text-[#b2de4f]" /><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-white/40">Recent activity</p></div>{recentTrades.length ? <div className="mt-3 space-y-2">{recentTrades.map((trade) => <div key={trade.id} className="flex items-center justify-between text-xs"><span className="truncate text-white/60">{trade.description}</span><span className="ml-2 shrink-0 text-white/35">{trade.status}</span></div>)}</div> : <p className="mt-4 text-xs text-white/35">No {symbol} trades yet.</p>}</section><section className="rounded-2xl border border-white/10 bg-[#111716] p-4"><div className="flex items-center gap-2"><WalletCards className="h-4 w-4 text-[#b2de4f]" /><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-white/40">Account snapshot</p></div><p className="mt-3 text-xl font-bold">KES {money(balance)}</p><p className="mt-1 text-xs text-white/35">Available demo margin</p><Link href="/app/deposit" className="mt-4 inline-flex items-center gap-2 text-xs font-bold text-[#b2de4f]">Manage wallet <ChevronDown className="h-3.5 w-3.5 -rotate-90" /></Link></section></div></div>{toast && <div role="status" className="fixed bottom-24 right-4 z-[70] flex max-w-[min(380px,calc(100vw-2rem))] items-start gap-3 rounded-2xl border border-white/10 bg-[#17201c] px-4 py-3 text-sm text-white shadow-2xl sm:bottom-6 sm:right-6"><CheckCircle2 className="mt-0.5 h-5 w-5 shrink-0 text-[#b2de4f]" /><span>{toast}</span><button type="button" onClick={() => setToast(null)} aria-label="Dismiss notification"><X className="h-4 w-4 text-white/45" /></button></div>}</main>;
}
