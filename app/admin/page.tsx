"use client";

import { ArrowLeft, Check, ShieldCheck, X } from "lucide-react";
import { Link } from "@/components/router-link";
import { useDemo } from "@/lib/demo/store";

export default function AdminPage() {
  const { transactions, kyc, reviewKyc: review, approveWithdrawal: approve } = useDemo(); const pendingWithdrawals = transactions.filter((item) => item.type === "WITHDRAWAL" && item.status === "PENDING"); const deposits = transactions.filter((item) => item.type === "DEPOSIT" && item.status === "COMPLETED").reduce((sum, item) => sum + item.amount, 0);
  return <main className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><Link href="/app" className="text-sm font-bold text-[#718071]"><ArrowLeft className="mr-2 inline h-4 w-4" />Back to app</Link><div className="mt-10 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#85a933]">Operations console</p><h2 className="mt-3 text-3xl font-bold tracking-[-0.04em]">Admin overview</h2></div><span className="rounded-full bg-[#fff6dc] px-3 py-2 text-xs font-bold text-[#987b2f]">Administrative controls</span></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-[#e2e8e0] bg-white p-5"><p className="text-xs font-bold uppercase tracking-[0.15em] text-[#8b938b]">Deposits</p><p className="mt-3 text-2xl font-bold">KES {deposits.toLocaleString()}</p></div><div className="rounded-2xl border border-[#e2e8e0] bg-white p-5"><p className="text-xs font-bold uppercase tracking-[0.15em] text-[#8b938b]">Pending withdrawals</p><p className="mt-3 text-2xl font-bold">{pendingWithdrawals.length}</p></div><div className="rounded-2xl border border-[#e2e8e0] bg-white p-5"><p className="text-xs font-bold uppercase tracking-[0.15em] text-[#8b938b]">KYC status</p><p className="mt-3 text-2xl font-bold">{kyc.status.replaceAll("_", " ")}</p></div></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><section className="rounded-3xl border border-[#e2e8e0] bg-white p-6"><div className="flex items-center gap-3"><ShieldCheck className="h-5 w-5 text-[#75a922]" /><h3 className="text-xl font-bold">KYC review</h3></div><p className="mt-3 text-sm text-[#7a827a]">Review the current identity verification submission.</p>{kyc.status === "PENDING" || kyc.status === "REQUIRES_REVIEW" ? <div className="mt-6 rounded-2xl bg-[#f7faf4] p-4"><p className="text-sm font-bold">{kyc.fullName || "Applicant"}</p><p className="mt-1 text-xs text-[#8b938b]">{kyc.country} · {kyc.documentType} · {kyc.documentNumber}</p><div className="mt-4 flex flex-wrap gap-2"><button onClick={() => review("APPROVED")} className="rounded-lg bg-[#b2de4f] px-3 py-2 text-xs font-bold text-[#24300f]"><Check className="mr-1 inline h-3.5 w-3.5" />Approve</button><button onClick={() => review("REQUIRES_REVIEW")} className="rounded-lg bg-[#fff6dc] px-3 py-2 text-xs font-bold text-[#987b2f]">Request info</button><button onClick={() => review("REJECTED")} className="rounded-lg bg-[#fff0f0] px-3 py-2 text-xs font-bold text-[#bd6c6c]"><X className="mr-1 inline h-3.5 w-3.5" />Reject</button></div></div> : <p className="mt-6 rounded-2xl bg-[#f7faf4] p-4 text-sm font-semibold text-[#748071]">No KYC submission requires review.</p>}</section><section className="rounded-3xl border border-[#e2e8e0] bg-white p-6"><h3 className="text-xl font-bold">Withdrawal approvals</h3><p className="mt-3 text-sm text-[#7a827a]">Approve pending payouts before they become completed ledger entries.</p><div className="mt-6 space-y-3">{pendingWithdrawals.length === 0 ? <p className="rounded-2xl bg-[#f7faf4] p-4 text-sm font-semibold text-[#748071]">No pending withdrawals.</p> : pendingWithdrawals.map((transaction) => <div key={transaction.id} className="flex items-center justify-between gap-4 rounded-2xl bg-[#f7faf4] p-4"><div><p className="text-sm font-bold">KES {transaction.amount.toLocaleString()}</p><p className="mt-1 text-xs text-[#8b938b]">{transaction.provider}</p></div><button onClick={() => approve(transaction.id)} className="rounded-lg bg-[#b2de4f] px-3 py-2 text-xs font-bold text-[#24300f]">Approve</button></div>)}</div></section></div></main>;
}
